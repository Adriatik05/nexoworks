'use client'

import { useState } from 'react'
import { ArrowRight, Check, Clock, MapPin } from 'lucide-react'

const treatments = [
  ['Dental Cleaning', '€30', 'A careful clinical clean and polish for a healthier smile.', '45 min'],
  ['Dental Filling', '€40+', 'Tooth-coloured restoration with a gentle, precise approach.', '60 min'],
  ['Teeth Whitening', '€120', 'Professional whitening with a natural, even result.', '90 min'],
  ['Dental Implant', '€650+', 'Long-term replacement planning from consultation to care.', '90 min'],
  ['Root Canal', '€100+', 'Comfort-first treatment to preserve your natural tooth.', '90 min'],
  ['Orthodontics', 'from €800', 'Personal treatment planning for a confident, balanced smile.', '30 min'],
]
const doctors = [['Dr. Arta Krasniqi', 'Dentist'], ['Dr. Leon Berisha', 'Oral Surgeon'], ['Dr. Elira Hoxha', 'Orthodontist']]

export function OrisDentalApp() {
  const [booking, setBooking] = useState(false)
  return <div className="theme-oris min-h-screen bg-[#f7faf9] text-[#152522] font-sans">
    <header className="sticky top-11 z-30 border-b border-[#152522]/10 bg-[#f7faf9]/90 backdrop-blur-md">
      <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8">
        <a href="/oris-dental" className="font-semibold tracking-tight">oris<span className="text-[#5e9187]"> / dental</span></a>
        <nav className="hidden gap-7 text-sm text-[#152522]/60 md:flex"><a href="#treatments">Treatments</a><a href="#doctors">Doctors</a><a href="#clinic">Clinic</a><a href="#faq">FAQ</a></nav>
        <button onClick={() => setBooking(true)} className="rounded-full bg-[#152522] px-5 py-2.5 text-sm text-white">Book appointment</button>
      </div>
    </header>
    <main>
      <section className="mx-auto grid max-w-7xl gap-12 px-5 py-16 lg:grid-cols-[1fr_.85fr] lg:px-8 lg:py-24">
        <div className="self-center"><p className="text-[11px] uppercase tracking-[.25em] text-[#5e9187]">Oris Dental · Prishtina</p><h1 className="mt-5 max-w-xl text-5xl font-semibold leading-[1.04] tracking-tight sm:text-7xl">Modern dentistry. Personal care.</h1><p className="mt-6 max-w-lg text-lg leading-relaxed text-[#152522]/60">Comprehensive dental care in a modern clinic in Prishtina. Clear advice, thoughtful treatment, and a team that remembers you.</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={() => setBooking(true)} className="rounded-full bg-[#152522] px-6 py-3 text-sm text-white">Book appointment</button><a href="#treatments" className="rounded-full border border-[#152522]/20 px-6 py-3 text-sm">Explore treatments</a></div><div className="mt-12 flex items-center gap-6 text-sm text-[#152522]/60"><span className="flex items-center gap-2"><MapPin className="size-4 text-[#5e9187]"/>Prishtina, Kosovo</span><span className="flex items-center gap-2"><Clock className="size-4 text-[#5e9187]"/>Mon–Sat · 08:00–19:00</span></div></div>
        <div className="relative min-h-[430px] overflow-hidden rounded-[2rem] bg-[#dcebe7] p-8"><img src="/oris/hero.png" alt="Oris Dental clinic" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute bottom-6 left-6 right-6 rounded-2xl bg-white/90 p-5 backdrop-blur"><p className="text-xs uppercase tracking-widest text-[#5e9187]">A calmer kind of care</p><p className="mt-2 text-xl font-medium">Your smile, on your terms.</p></div></div>
      </section>
      <section id="treatments" className="border-y border-[#152522]/10 bg-white"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><p className="text-xs uppercase tracking-[.25em] text-[#5e9187]">What we do</p><div className="mt-4 flex flex-col justify-between gap-5 md:flex-row md:items-end"><h2 className="max-w-lg text-4xl font-semibold tracking-tight">Care that is clear, considered, and comfortable.</h2><p className="max-w-sm text-sm leading-relaxed text-[#152522]/55">From your first visit to long-term treatment, we make every step understandable.</p></div><div className="mt-12 grid gap-px border border-[#152522]/10 bg-[#152522]/10 sm:grid-cols-2 lg:grid-cols-3">{treatments.map(([name, price, desc, duration]) => <button key={name} onClick={() => setBooking(true)} className="group bg-white p-6 text-left transition-colors hover:bg-[#eef6f3]"><div className="flex items-start justify-between gap-4"><h3 className="font-medium">{name}</h3><span className="text-sm text-[#5e9187]">{price}</span></div><p className="mt-5 text-sm leading-relaxed text-[#152522]/55">{desc}</p><p className="mt-6 flex items-center gap-2 text-xs text-[#152522]/45"><Clock className="size-3.5"/>{duration}<ArrowRight className="ml-auto size-4 transition-transform group-hover:translate-x-1"/></p></button>)}</div></div></section>
      <section id="doctors" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><p className="text-xs uppercase tracking-[.25em] text-[#5e9187]">Meet the team</p><h2 className="mt-4 text-4xl font-semibold tracking-tight">Experienced hands. Human approach.</h2><div className="mt-10 grid gap-4 md:grid-cols-3">{doctors.map(([name, role], i) => <div key={name} className="border border-[#152522]/10 p-6"><div className="flex size-12 items-center justify-center rounded-full bg-[#dcebe7] text-lg font-medium text-[#5e9187]">{name.split(' ').slice(-1)[0][0]}</div><h3 className="mt-6 font-medium">{name}</h3><p className="mt-1 text-sm text-[#152522]/55">{role}</p><button onClick={() => setBooking(true)} className="mt-8 text-sm underline underline-offset-4">Book with {name.split(' ')[1]}</button></div>)}</div></section>
      <section id="clinic" className="bg-[#152522] px-5 py-20 text-white lg:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-8 md:flex-row md:items-end"><div><p className="text-xs uppercase tracking-[.25em] text-[#a8d4c9]">A modern clinic in Prishtina</p><h2 className="mt-4 max-w-xl text-4xl font-semibold tracking-tight">Space to feel looked after.</h2></div><button onClick={() => setBooking(true)} className="rounded-full bg-white px-6 py-3 text-sm text-[#152522]">Book your visit</button></div></section>
      <section id="faq" className="mx-auto max-w-3xl px-5 py-20"><p className="text-xs uppercase tracking-[.25em] text-[#5e9187]">Questions</p>{[['Do you accept new patients?', 'Yes. We welcome new patients and can usually arrange an initial consultation within the same week.'], ['How do I prepare for my appointment?', 'Bring any recent dental records or medication details, and arrive ten minutes early so we can complete your patient profile.'], ['Do you offer payment plans?', 'For larger treatments such as implants and orthodontics, our team can explain staged payment options during your consultation.']].map(([q, answer]) => <details key={q} className="border-b border-[#152522]/10 py-5"><summary className="cursor-pointer font-medium">{q}</summary><p className="mt-3 text-sm leading-relaxed text-[#152522]/60">{answer}</p></details>)}</section>
    </main>
    {booking && <BookingModal onClose={() => setBooking(false)} />}
  </div>
}

function BookingModal({ onClose }: { onClose: () => void }) {
  const [step, setStep] = useState(0); const [done, setDone] = useState(false); const [treatment, setTreatment] = useState(treatments[0][0]); const [doctor, setDoctor] = useState(doctors[0][0]); const [date, setDate] = useState(''); const [time, setTime] = useState(''); const [name, setName] = useState(''); const [phone, setPhone] = useState(''); const [email, setEmail] = useState('');
  const valid = [!!treatment, !!doctor, !!date && !!time, name.length > 1 && phone.length >= 6 && email.includes('@')][step]
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#152522]/40 p-0 sm:items-center sm:p-5"><div className="max-h-[90vh] w-full max-w-lg overflow-auto rounded-t-3xl bg-white p-6 sm:rounded-3xl sm:p-8">{done ? <div className="py-10 text-center"><div className="mx-auto flex size-14 items-center justify-center rounded-full bg-[#dcebe7]"><Check className="size-7 text-[#5e9187]"/></div><h2 className="mt-6 text-2xl font-semibold">Appointment confirmed</h2><p className="mt-3 text-sm text-[#152522]/60">We&apos;ll contact you to confirm your visit at Oris Dental.</p><button onClick={onClose} className="mt-8 rounded-full bg-[#152522] px-6 py-3 text-sm text-white">Done</button></div> : <><div className="flex items-start justify-between"><div><p className="text-xs uppercase tracking-widest text-[#5e9187]">Step {step + 1} of 4</p><h2 className="mt-2 text-2xl font-semibold">Book an appointment</h2></div><button onClick={onClose} className="text-sm text-[#152522]/50">Close</button></div><div className="mt-8">{step === 0 && <select value={treatment} onChange={e => setTreatment(e.target.value)} className="h-12 w-full rounded-xl border border-[#152522]/15 px-4"><option disabled>Choose treatment</option>{treatments.map(t => <option key={t[0]}>{t[0]} · {t[1]}</option>)}</select>}{step === 1 && <select value={doctor} onChange={e => setDoctor(e.target.value)} className="h-12 w-full rounded-xl border border-[#152522]/15 px-4">{doctors.map(d => <option key={d[0]}>{d[0]} · {d[1]}</option>)}</select>}{step === 2 && <div className="space-y-4"><label className="block text-sm">Date<input type="date" value={date} onChange={e => setDate(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-[#152522]/15 px-4"/></label><div className="grid grid-cols-3 gap-2">{['09:00','10:30','12:00','14:30','16:00','17:30'].map(t => <button key={t} onClick={() => setTime(t)} className={`h-11 rounded-xl border text-sm ${time === t ? 'border-[#152522] bg-[#152522] text-white' : 'border-[#152522]/15'}`}>{t}</button>)}</div></div>}{step === 3 && <div className="space-y-3"><input placeholder="Full name" value={name} onChange={e => setName(e.target.value)} className="h-12 w-full rounded-xl border border-[#152522]/15 px-4"/><input placeholder="Phone" value={phone} onChange={e => setPhone(e.target.value)} className="h-12 w-full rounded-xl border border-[#152522]/15 px-4"/><input placeholder="Email" type="email" value={email} onChange={e => setEmail(e.target.value)} className="h-12 w-full rounded-xl border border-[#152522]/15 px-4"/><textarea placeholder="Optional note" className="min-h-24 w-full rounded-xl border border-[#152522]/15 px-4 py-3"/></div>}</div><div className="mt-8 flex justify-between"><button disabled={step === 0} onClick={() => setStep(step - 1)} className="text-sm disabled:opacity-0">Back</button>{step < 3 ? <button disabled={!valid} onClick={() => setStep(step + 1)} className="rounded-full bg-[#152522] px-6 py-3 text-sm text-white disabled:opacity-40">Continue</button> : <button disabled={!valid} onClick={() => setDone(true)} className="rounded-full bg-[#152522] px-6 py-3 text-sm text-white disabled:opacity-40">Confirm appointment</button>}</div></>}</div></div>
}

export default OrisDentalApp
